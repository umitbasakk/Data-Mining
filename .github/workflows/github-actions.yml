name: Github Actions
run-name: Started deploying
on:
  push:
    branches:
      - Backend
jobs:
  Deployin:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4